name: Sync Data to Cloudflare KV

on:
  workflow_run:
    workflows: ["crawl"]
    types:
      - completed
  workflow_dispatch: # 保留手动触发按钮，方便调试

jobs:
  upload:
    runs-on: ubuntu-latest
    # 只有当前置爬虫任务成功时才运行
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Deploy to Cloudflare KV
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          packageManager: npm
          # 直接写子命令，使用最新的 HOK_DATA 绑定
          command: |
            kv:key put --binding=HOK_DATA "hero_list" data/hero/list.json --path
            kv:key put --binding=HOK_DATA "item_list" data/item/list.json --path
            kv:key put --binding=HOK_DATA "summoner_list" data/summoner/list.json --path
            kv:key put --binding=HOK_DATA "hero_images" data/hero_images.json --path
            kv:key put --binding=HOK_DATA "item_images" data/item_images.json --path
            kv:key put --binding=HOK_DATA "summoner_images" data/summoner_images.json --path
